mad_comment_homepage:
    pattern:  /hello/{name}
    defaults: { _controller: MADCommentBundle:Default:index }

fos_comment_api:
    type: rest
    resource: "@FOSCommentBundle/Resources/config/routing.yml"
    prefix: /api
    defaults: { _format: html }
